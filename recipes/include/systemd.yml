type: systemd
system:
  disabled:
    - flatpak-add-fedora-repos.service
  enabled:
    - bootc-fetch-updates.timer
user:
  enabled:
    - flatpak-user-update.timer
    - podman.service
    - podman.socket
