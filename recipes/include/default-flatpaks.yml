type: default-flatpaks@v2
configurations:
  - scope: user
    repo:
      title: Flathub (User)
  - notify: true
    install:
      - org.gnome.Calculator
      - org.gnome.TextEditor
      - org.mozilla.firefox
